apiVersion: apps/v1
kind: Deployment
metadata:
  name: lstm-transformer-deployment
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      service: lstm-transformer
  template:
    metadata:
      labels:
        service: lstm-transformer
    spec:
      containers:
       - name: lstm-container
         image: sururikus/deneme2:main
         imagePullPolicy: Always
         env:
          - name: POSTGRES_PASSWORD
            value: mysecretpassword

       - name: transformer-container
         image: sururikus/deneme2:latest
         imagePullPolicy: Always
         env:
          - name: POSTGRES_PASSWORD
            value: mysecretpassword

---
apiVersion: v1
kind: Service
metadata:
  name: service-entrypoint
  namespace: default
spec:
  type: NodePort
  selector:
    service: lstm-transformer
  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 30001
